# Ambientes locais
.venv/
venv/
__pycache__/
*.pyc

# Arquivos gerados temporarios
.pytest_cache/
.mypy_cache/
*.tmp
*.bak

# Dados sensiveis devem ficar fora da arvore do repositorio
dados-privados/
enderecos/
pagamentos/
*.cpf
*.secret
*.env

# Exportacoes locais
exports/
relatorios-privados/

# Relatorios de cobertura
.coverage
coverage.json
htmlcov/

# Trava de escrita concorrente
.central-compras.lock

# Snapshots do navegador automatizado (MCP). Guardam a pagina inteira: ja
# capturaram token de Apps Script, CEP e numero com cara de cartao vindos das
# lojas. Tambem esta no gitignore global desta maquina, mas nao da pra contar
# com isso em outra maquina - por isso fica aqui tambem.
.playwright-mcp/
